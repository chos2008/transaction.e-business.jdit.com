<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
    "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="req">
	<resultMap type="Requirement" id="Requirement">
		<id column="id" property="id"/>
		<result column="user_id" property="userId"/>
		<result column="title" property="title"/>
		<result column="amount" property="amount"/>
		<result column="content" property="content"/>
		<result column="start_time" property="startTime"/>
		<result column="end_time" property="endTime"/>
		<result column="creation" property="creation"/>
		<result column="updation" property="updation"/>
	</resultMap>
	
	<select id="item-list-r" resultMap="Requirement">
		select * from requirement 
		<if test="userId != null">
			where user_id = #{userId} order by creation desc
		</if>
		limit #{firstResult}, #{maxResultSize}
	</select>
	
	
	<select id="get-item" resultMap="Requirement" parameterType="int">
		select * from requirement where id = #{id}
	</select>
	
	
	<insert id="issue" parameterType="Requirement">
		<selectKey keyProperty="id" resultType="int">
			select @@identity as id 
		</selectKey>
		insert into requirement(
			id
			,user_id
			,title
			,amount
			<if test="content != null">
				,content
			</if>
			<if test="startTime != null">
				,start_time
			</if>
			<if test="endTime != null">
				,end_time
			</if>
			,creation
			<if test="updation != null">
				,updation
			</if>
		) values(
			#{id}
			,#{userId}
			,#{title}
			,#{amount}
			<if test="content != null">
				,#{content}
			</if>
			<if test="startTime != null">
				,#{startTime}
			</if>
			<if test="endTime != null">
				,#{endTime}
			</if>
			,#{creation}
			<if test="updation != null">
				,#{updation}
			</if>
		)
	</insert>
</mapper>